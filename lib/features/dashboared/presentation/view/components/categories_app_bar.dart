import 'package:flutter/material.dart';

AppBar  categoriesAppBar ({required String titel})=> AppBar(
              backgroundColor: Colors.white,
              title:  Text(titel),
              centerTitle: true,
            );